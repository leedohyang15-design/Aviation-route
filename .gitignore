node_modules/
out/
dist/
.DS_Store
*.log
.env
.env.local

# Route cache written next to a packaged build at runtime
aviation-route-routes.json

# Orbital elements cached next to a packaged build at runtime
aviation-route-tle.txt
